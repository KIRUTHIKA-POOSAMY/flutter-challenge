import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFFD3E0E4);
  static const surface = Color(0xFFF7F6F4);
  static const card = Colors.white;

  // Text
  static const primaryText = Color(0xFF1E1E1E);
  static const secondaryText = Color(0xFF6E6A66);
  static const lightText = Color(0xFFA3A09B);

  // Home screen cards
  static const blueCard = Color(0xFFD4EDF5);
  static const greenCard = Color(0xFFD5EBAF);

  // Calendar / peach
  static const peach = Color(0xFFF2C0A8);

  // General blue
  static const blue = Color(0xFFD8EEF5);

  // Green
  static const green = Color(0xFFC8DEA0);

  // Home screen chart
  static const chartBlue = Color(0xFF3FA4D5);

  // Workout icon
  static const iconBlue = Color(0xFF3D7AB8);

  // Gauge
  static const gaugeBlue = Color(0xFFCBEAF6);
  static const gaugeGrey = Color(0xFFE8E8E4);
  static const gaugeOrange = Color(0xFFF39A1E);
  static const gaugeYellow = Color(0xFFE5B321);
  static const gaugeGreen = Color(0xFF98C73C);

  // Heart
  static const heart = Color(0xFFE6A081);

  // Notification
  static const notification = Color(0xFFFF3A30);
}
